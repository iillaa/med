
# Dependencies
node_modules/

# Temp files
*.tmp
apktool_output/
apktool_output_new/
audit_output/
audit_output_new/

# Local backups (tracked by Git history instead)
*.bak

# Kilo agent-manager / worktree internals
.kilo/

# IDE & Tool configurations
.antigravitycli/
.gemini/
.DS_Store
.cat-med/reference-pdfs

# Uncompressed Master Original PDFs (Local server only, keep off GitHub to minimize repo/APK size)
data/pdf_masters/
data/pdf_sliced/
data/pdf_done/

# Local ngrok binary
ngrok

# Runtime server-provider link (gitignored so the tunnel URL is never committed)
remote_server_config.json

# Environment & API keys
.env
*.env

# Local web research cache
cat_db_generator/web_cache/
data/web_cache/
scratch/
data/*_bdpm.txt
data/*.csv

# Logs
*.log
nohup.out

# Suggestions database & device telemetry
suggestions.json*
server/data/active_devices.json
!remote_server_config.json

# Admin Password
admin_password.txt*

# Build artifact: esbuild app bundle (regenerated by `npm run build`)
public/dist/

# Generated server-providers CommonJS shim
public/js/server-providers.cjs

# Backups and audit logs
backups/
audit.log

# Lighthouse CI local reports (also produced by CI artifact upload)
.lighthouseci/

# PM2 log files
logs/

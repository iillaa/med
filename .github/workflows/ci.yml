name: Node.js CI

on:
  push:
    branches: [ master, structured ]
  pull_request:
    branches: [ master, structured ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Run Full Automated Test Suite
      run: npm run test:suite

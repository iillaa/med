# Stage 1 Finalization Checklist

- `[ ]` **1. Quiz Setup Contrast Fixes**
  - `[ ]` Clean up inline labels and select box color styling overrides in `index.html`.
- `[ ]` **2. Contextual Notes Persistence Warning**
  - `[ ]` Add a disclaimer subtext under the notes editor text area in `index.html`.
- `[ ]` **3. PWA Integration**
  - `[ ]` Add manifest/theme meta headers in `index.html`.
  - `[ ]` Create `manifest.json`.
  - `[ ]` Create `service-worker.js`.
  - `[ ]` Register the Service Worker on startup in `main.js`.

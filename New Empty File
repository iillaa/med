Kilo, I need a snapshot of the current project state for a senior developer to verify before starting Phase 3.

Please output the **raw content** (or full text) of the following files, and the directory listing. Do not summarize – I need the actual data.

**1. Root Directory Structure:**
Run `ls -la` from the project root and paste the output.

**2. Updated Reconstruction Plan:**
Output the full content of `RECONSTRUCTION_PLAN.md` (the one you updated to use Vue 3).

**3. Root package.json:**
Output the full content of `package.json` (from the project root, not the vue one).

**4. Server Entry Point:**
Output the full content of `server/index.js` (or `server.js`, whichever is currently the main server file).

**5. Current Frontend Status:**
Check if `med-vue/` folder exists in the root. If yes, tell me what's inside (just `ls -la med-vue/`). If not, just say "med-vue does not exist".

Output all of this in a single response. Keep the output raw so I can parse it easily.